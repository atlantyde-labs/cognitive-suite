# Security & Compliance

ATLANTYQA integra la seguridad en la operación diaria:
- SBOM/SCA dentro de los pipelines (`release-draft.yml`, `jsonl-validate-wizard.sh`).
- Logs y evidencias en `bot-review.yml` y `outputs/bot-evidence`.
- Hardening de runtime (documentado en `docs/internal/examples/hardening-note.md`).
- Flujos de datos seguros que el deployment model describe en `docs/trust/deployment-models.md`.
