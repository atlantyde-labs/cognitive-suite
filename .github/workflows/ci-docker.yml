name: CI Docker/Compose

on:
  workflow_call:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      # Ensure we are authenticated to GHCR before attempting to push.
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          # If the reusable workflow caller can't grant `packages: write` to GITHUB_TOKEN,
          # create a PAT with `write:packages` (and repo if needed) and store it as
          # a repo secret (e.g. GHCR_PAT) and use:
          # password: ${{ secrets.GHCR_PAT }}

      - name: Build frontend image (PR) / Build+Push (main)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: |
            ghcr.io/${{ github.repository }}/cognitive-frontend:latest
            ghcr.io/${{ github.repository }}/cognitive-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Prepare bootstrap env
        run: |
          python - <<'PY'
          import secrets
          from pathlib import Path

          salt = secrets.token_hex(16)
          Path(".env.bootstrap").write_text(
              f"COGNITIVE_HASH_SALT={salt}\n"
              "COGNITIVE_ENV=prod\n"
              "COGNITIVE_REDACT=1\n"
          )
          PY

      - name: Docker compose E2E (playground)
        run: |
          BOOTSTRAP_ENV_FILE=.env.bootstrap \
          BOOTSTRAP_REDACT=1 \
          BOOTSTRAP_DEBUG=1 \
          BOOTSTRAP_COMPOSE_FILE=docker-compose.playground.yml \
          BOOTSTRAP_TIMEOUT_SECS=900 \
          bash test-bootstrap.sh

      - name: Docker compose teardown
        if: always()
        run: |
          docker compose down -v --remove-orphans
