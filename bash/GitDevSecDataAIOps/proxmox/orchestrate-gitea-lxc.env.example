# ==================================================
# Gitea LXC Orchestration Configuration
# ==================================================
#
# Este fichero centraliza la configuración para el script 'orchestrate-gitea-lxc.sh'.
# Rellena los valores y pásalo como argumento al script.

# --- Configuración del Wizard de Secretos ---
# El wizard generará los ficheros .env detallados en este directorio.
# Ver 'proxmox-local-secrets-wizard.env.example' para más detalles.
SECRETS_OUTPUT_DIR="./outputs/secrets"
DRY_RUN="true" # Cambiar a "false" para ejecutar cambios reales

# --- Configuración de la API de Proxmox (PVE) ---
PVE_API_URL="https://<proxmox_ip>:8006/api2/json"
PVE_API_TOKEN="root@pam!mytoken=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
PVE_INSECURE="true" # Aceptar certificados autofirmados

# --- Configuración del Despliegue del LXC de Gitea ---
# Ver 'deploy-gitea-lxc-api.env.example' para más detalles.
LXC_ID="123" # ID del contenedor LXC a crear
LXC_HOSTNAME="gitea-server"
LXC_PASSWORD="<una_contraseña_segura_para_root_del_lxc>"
LXC_CORES="2"
LXC_MEMORY="2048" # en MB
LXC_DISK="20" # en GB
LXC_STORAGE="local-lvm" # Pool de almacenamiento en Proxmox
LXC_BRIDGE="vmbr0"
LXC_TEMPLATE="local:vztmpl/debian-12-standard_12.2-1_amd64.tar.zst"

# --- Configuración de Gitea ---
# Esta URL se usará para configurar Gitea internamente.
GITEA_URL="http://${LXC_HOSTNAME}:3000"
GITEA_TOKEN="" # Token de admin. Dejar vacío en el primer despliegue.
