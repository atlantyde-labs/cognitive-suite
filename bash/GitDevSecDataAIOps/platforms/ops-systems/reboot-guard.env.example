RG_WINDOW_START="02:00"
RG_WINDOW_END="03:00"
RG_WINDOW_DAYS="Sun"
RG_CHECK_INTERVAL="60"

RG_INHIBIT_WHAT="shutdown"
RG_INHIBIT_MODE="block"
RG_INHIBIT_WHO="reboot-guard"
RG_INHIBIT_WHY="Reboot guard active"

# Optional override file: if present and newer than RG_OVERRIDE_TTL minutes, reboots are allowed.
RG_OVERRIDE_FILE="/run/reboot-guard/allow"
RG_OVERRIDE_TTL="30"
