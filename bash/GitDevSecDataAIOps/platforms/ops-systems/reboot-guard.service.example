[Unit]
Description=Reboot guard (blocks reboot outside window)
After=network.target

[Service]
Type=simple
EnvironmentFile=-/etc/reboot-guard.env
ExecStart=/usr/local/bin/reboot-guard.sh
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
